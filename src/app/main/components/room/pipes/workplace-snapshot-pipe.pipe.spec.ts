import { WorkplaceSnapshotPipePipe } from './workplace-snapshot.pipe';

describe('WorkplaceSnapshotPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkplaceSnapshotPipePipe();
    expect(pipe).toBeTruthy();
  });
});
